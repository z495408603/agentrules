# 示例模板包

这里放的是可直接参考的 `agentrules.config.json` 示例集合。

当前包含：
- `nextjs-team`：偏前端团队协作，强调 App Router、组件边界和测试。
- `react-library`：偏组件库/设计系统项目，强调组件 API 和变更影响。
- `node-service`：偏后端服务，强调接口契约、日志和回归验证。

这些示例目前是仓库内模板包，后续可以继续演化成独立 preset 包。
